apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: paperless-auth
  namespace: paperless
spec:
  method: kubernetes
  mount: kubernetes
  kubernetes:
    role: paperless-role # This must match the role name in Vault
    serviceAccount: default
