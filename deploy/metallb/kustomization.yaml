apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # 1. The official software install
  - https://raw.githubusercontent.com/metallb/metallb/v0.15.3/config/manifests/metallb-native.yaml
  # 2. Your custom IP pool and advertisement
  - config.yaml

# This ensures everything lands in the right place
namespace: metallb-system
